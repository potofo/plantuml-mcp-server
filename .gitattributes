# Normalize line endings: commit LF, keep LF in working copies on all
# platforms (shell scripts and Docker/CI files break with CRLF).
* text=auto eol=lf

# Binary assets
*.png binary
*.otf binary
*.jar binary
